name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      - name: Run Ruff linter
        run: ruff check src/

      - name: Run Ruff formatter check
        run: ruff format --check src/

  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Install Playwright browsers
        run: playwright install --with-deps chromium

      - name: Run tests with coverage
        run: |
          coverage run -m pytest tests/ -v
          coverage combine || true

      - name: Check per-file coverage thresholds
        run: python scripts/check_coverage.py
        if: hashFiles('.coverage-baseline.json') != ''

      - name: Upload coverage data
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-data
          path: .coverage
          if-no-files-found: warn

  complexity:
    name: Complexity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install radon xenon

      - name: Check cyclomatic complexity (xenon)
        # TODO: Tighten thresholds as code is refactored
        # CURRENT: --max-absolute D --max-modules B --max-average B (relaxed)
        # TARGET:  --max-absolute C --max-modules A --max-average A (strict)
        #
        # Grades: A=1-5, B=6-10, C=11-20, D=21-30, E=31-40, F=41+
        run: xenon --max-absolute D --max-modules B --max-average B src/demorec/

      - name: Show complexity metrics (radon)
        run: radon cc src/demorec/ -a -s

  function-length:
    name: Function Length
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check function length
        # TODO: Tighten thresholds as code is refactored
        # CURRENT: --warn 20 --error 65 (relaxed for initial adoption)
        # TARGET:  --warn 8 --error 12 (strict - encourages small functions)
        #
        # Use "# length-ok" comment on def line to exempt specific functions
        run: python scripts/check_function_length.py src/ --warn 20 --error 65

  quality-report:
    name: Quality Report
    runs-on: ubuntu-latest
    needs: [test, complexity, function-length]
    # Run when jobs complete (pass or fail) but not when cancelled/skipped
    if: (success() || failure()) && github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install radon

      - name: Download coverage data
        uses: actions/download-artifact@v4
        with:
          name: coverage-data
        continue-on-error: true

      - name: Generate quality report
        run: python scripts/quality_report.py -o quality-report.md

      - name: Post quality report to PR
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: quality-report
          path: quality-report.md
